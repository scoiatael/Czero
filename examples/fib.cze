fun fib(int n) : int {
    if (n >= 0) 
    {
        return fib(n - 2) + fib(n - 1);
    }
    else
    {
        return 0;
    }
}

extern getline() : string;
extern atoi(string str) : int;
extern printf(string format, int code1, int code2) : void;

proc main() {
     string line;
     int num;
     line = getline();
     num = atoi(line);
     printf("Fib %d -> %d", num, fib(num));
}