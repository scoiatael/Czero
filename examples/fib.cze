fun fib(n : int) : int {
    if (n >= 0) // comparison operators are overloaded functions that return bool
        return fib(n - 2) + fib(n - 1);
    else
        return 0;
    // how to make sure function always returns with a value ?
    // one needs to perform some abstract interpretation!
}
