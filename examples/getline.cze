extern printf(string format, string out) : void;
extern malloc(int length) : string;
extern scanf(string format, string out) : void;

proc main() {
  string s;
  s = malloc(123);
  scanf("%s", s);
  printf("READ: %s\n", s);
}